AWSTemplateFormatVersion: '2010-09-09'
Description: "S3 Storage and DynamoDB Tables"

Parameters:
  ProjectNamespace:
    Type: String
    Default: nusiss-team-1-voc
  Environment:
    Type: String
    Default: ""
    Description: "Environment prefix (e.g. prod). Leave empty for dev."

Conditions:
  HasEnvPrefix: !Not [!Equals [!Ref Environment, ""]]

Resources:

  AssetsBucket:
    Type: AWS::S3::Bucket
    Properties:
      BucketName: !If
        - HasEnvPrefix
        - !Sub "${ProjectNamespace}-${Environment}-assets"
        - !Sub "${ProjectNamespace}-assets"
      BucketEncryption:
        ServerSideEncryptionConfiguration:
          - ServerSideEncryptionByDefault:
              SSEAlgorithm: AES256
      PublicAccessBlockConfiguration:
        BlockPublicAcls: true
        BlockPublicPolicy: true
        IgnorePublicAcls: true
        RestrictPublicBuckets: true

  AssetsBucketPolicy:
    Type: AWS::S3::BucketPolicy
    Properties:
      Bucket: !Ref AssetsBucket
      PolicyDocument:
        Version: "2012-10-17"
        Statement:
          - Sid: AllowCloudFrontServicePrincipal
            Effect: Allow
            Principal:
              Service: cloudfront.amazonaws.com
            Action: s3:GetObject
            Resource: !Sub "${AssetsBucket.Arn}/*"
            Condition:
              StringEquals:
                "AWS:SourceArn": !Sub "arn:aws:cloudfront::${AWS::AccountId}:distribution/${AssetsCDN}"


  AssetsCDNOriginAccessControl:
    Type: AWS::CloudFront::OriginAccessControl
    Properties:
      OriginAccessControlConfig:
        Name: !If
          - HasEnvPrefix
          - !Sub "${ProjectNamespace}-${Environment}-assets-oac"
          - !Sub "${ProjectNamespace}-assets-oac"
        OriginAccessControlOriginType: s3
        SigningBehavior: always
        SigningProtocol: sigv4

  AssetsCDN:
    Type: AWS::CloudFront::Distribution
    Properties:
      DistributionConfig:
        Enabled: true
        Comment: !If
          - HasEnvPrefix
          - !Sub "${ProjectNamespace}-${Environment} - S3 Assets CDN"
          - !Sub "${ProjectNamespace} - S3 Assets CDN"
        DefaultCacheBehavior:
          TargetOriginId: S3AssetsOrigin
          ViewerProtocolPolicy: redirect-to-https
          CachePolicyId: 658327ea-f89d-4fab-a63d-7e88639e58f6 # CachingOptimized
          Compress: true
          AllowedMethods:
            - GET
            - HEAD
          CachedMethods:
            - GET
            - HEAD
        Origins:
          - Id: S3AssetsOrigin
            DomainName: !GetAtt AssetsBucket.RegionalDomainName
            OriginAccessControlId: !Ref AssetsCDNOriginAccessControl
            S3OriginConfig:
              OriginAccessIdentity: ""
        HttpVersion: http2
        PriceClass: PriceClass_200


  GameCardLookup:
    Type: AWS::DynamoDB::Table
    DeletionPolicy: Delete
    UpdateReplacePolicy: Delete
    Properties:
      TableName: !If
        - HasEnvPrefix
        - !Sub "${Environment}-GameCardLookup"
        - GameCardLookup
      BillingMode: PAY_PER_REQUEST
      AttributeDefinitions:
        - AttributeName: gameId
          AttributeType: S
        - AttributeName: meta
          AttributeType: S
      KeySchema:
        - AttributeName: gameId
          KeyType: HASH
        - AttributeName: meta
          KeyType: RANGE
      PointInTimeRecoverySpecification:
        PointInTimeRecoveryEnabled: true
      SSESpecification:
        SSEEnabled: true
      StreamSpecification:
        StreamViewType: NEW_AND_OLD_IMAGES

  MessagingPlatform:
    Type: AWS::DynamoDB::Table
    DeletionPolicy: Delete
    UpdateReplacePolicy: Delete
    Properties:
      TableName: !If
        - HasEnvPrefix
        - !Sub "${Environment}-MessagingPlatform"
        - MessagingPlatform
      BillingMode: PAY_PER_REQUEST
      AttributeDefinitions:
        - AttributeName: entityId
          AttributeType: S
        - AttributeName: entityType
          AttributeType: S
        - AttributeName: lastMessageAt
          AttributeType: N
        - AttributeName: createdAt
          AttributeType: N
      KeySchema:
        - AttributeName: entityId
          KeyType: HASH
        - AttributeName: entityType
          KeyType: RANGE
      PointInTimeRecoverySpecification:
        PointInTimeRecoveryEnabled: true
      SSESpecification:
        SSEEnabled: true
      StreamSpecification:
        StreamViewType: NEW_AND_OLD_IMAGES
      GlobalSecondaryIndexes:
        - IndexName: RoomIndex
          KeySchema:
            - AttributeName: entityId
              KeyType: HASH
            - AttributeName: lastMessageAt
              KeyType: RANGE
          Projection:
            ProjectionType: ALL
        - IndexName: MessageIndex
          KeySchema:
            - AttributeName: entityId
              KeyType: HASH
            - AttributeName: createdAt
              KeyType: RANGE
          Projection:
            ProjectionType: ALL

  TCGMarketplace:
    Type: AWS::DynamoDB::Table
    DeletionPolicy: Delete
    UpdateReplacePolicy: Delete
    Properties:
      TableName: !If
        - HasEnvPrefix
        - !Sub "${Environment}-TCGMarketplace"
        - TCGMarketplace
      BillingMode: PAY_PER_REQUEST
      AttributeDefinitions:
        - AttributeName: gameName
          AttributeType: S
        - AttributeName: sellerId
          AttributeType: S
        - AttributeName: listingId
          AttributeType: S
        - AttributeName: listingUpdatedAt
          AttributeType: S
        - AttributeName: listingCardName
          AttributeType: S
        - AttributeName: listingPrice
          AttributeType: S
      KeySchema:
        - AttributeName: gameName
          KeyType: HASH
        - AttributeName: listingId
          KeyType: RANGE
      PointInTimeRecoverySpecification:
        PointInTimeRecoveryEnabled: true
      SSESpecification:
        SSEEnabled: true
      StreamSpecification:
        StreamViewType: NEW_AND_OLD_IMAGES
      GlobalSecondaryIndexes:
        - IndexName: SellerListingIndex
          KeySchema:
            - AttributeName: sellerId
              KeyType: HASH
            - AttributeName: listingUpdatedAt
              KeyType: RANGE
          Projection:
            ProjectionType: ALL
        - IndexName: UpdatedListingIndex
          KeySchema:
            - AttributeName: gameName
              KeyType: HASH
            - AttributeName: listingUpdatedAt
              KeyType: RANGE
          Projection:
            ProjectionType: ALL
        - IndexName: CardListingIndex
          KeySchema:
            - AttributeName: gameName
              KeyType: HASH
            - AttributeName: listingCardName
              KeyType: RANGE
          Projection:
            ProjectionType: ALL
        - IndexName: PriceListingIndex
          KeySchema:
            - AttributeName: gameName
              KeyType: HASH
            - AttributeName: listingPrice
              KeyType: RANGE
          Projection:
            ProjectionType: ALL

  UserProfile:
    Type: AWS::DynamoDB::Table
    DeletionPolicy: Delete
    UpdateReplacePolicy: Delete
    Properties:
      TableName: !If
        - HasEnvPrefix
        - !Sub "${Environment}-UserProfile"
        - UserProfile
      BillingMode: PAY_PER_REQUEST
      AttributeDefinitions:
        - AttributeName: userId
          AttributeType: S
      KeySchema:
        - AttributeName: userId
          KeyType: HASH
      PointInTimeRecoverySpecification:
        PointInTimeRecoveryEnabled: true
      SSESpecification:
        SSEEnabled: true

Outputs:
  # Storage
  AssetsBucketName:
    Description: "The name of the S3 assets bucket"
    Value: !Ref AssetsBucket
    Export:
      Name: !If
        - HasEnvPrefix
        - !Sub "${ProjectNamespace}-${Environment}-AssetsBucketName"
        - !Sub "${ProjectNamespace}-AssetsBucketName"
  AssetsBucketArn:
    Description: "The ARN of the S3 assets bucket"
    Value: !GetAtt AssetsBucket.Arn
    Export:
      Name: !If
        - HasEnvPrefix
        - !Sub "${ProjectNamespace}-${Environment}-AssetsBucketArn"
        - !Sub "${ProjectNamespace}-AssetsBucketArn"
  AssetsCDNDomainName:
    Description: "CloudFront distribution domain for S3 assets"
    Value: !GetAtt AssetsCDN.DomainName
    Export:
      Name: !If
        - HasEnvPrefix
        - !Sub "${ProjectNamespace}-${Environment}-AssetsCDNDomainName"
        - !Sub "${ProjectNamespace}-AssetsCDNDomainName"
  AssetsCDNUrl:
    Description: "CloudFront URL for S3 assets"
    Value: !Sub "https://${AssetsCDN.DomainName}"
    Export:
      Name: !If
        - HasEnvPrefix
        - !Sub "${ProjectNamespace}-${Environment}-AssetsCDNUrl"
        - !Sub "${ProjectNamespace}-AssetsCDNUrl"

  # Database
  GameCardLookupTableName:
    Description: "The name of the DynamoDB table GameCardLookup"
    Value: !Ref GameCardLookup
    Export:
      Name: !If
        - HasEnvPrefix
        - !Sub "${ProjectNamespace}-${Environment}-GameCardLookupTableName"
        - !Sub "${ProjectNamespace}-GameCardLookupTableName"
  GameCardLookupTableArn:
    Description: "The ARN of the DynamoDB table GameCardLookup"
    Value: !GetAtt GameCardLookup.Arn
    Export:
      Name: !If
        - HasEnvPrefix
        - !Sub "${ProjectNamespace}-${Environment}-GameCardLookupTableArn"
        - !Sub "${ProjectNamespace}-GameCardLookupTableArn"
  MessagingPlatformTableName:
    Description: "The name of the DynamoDB table MessagingPlatform"
    Value: !Ref MessagingPlatform
    Export:
      Name: !If
        - HasEnvPrefix
        - !Sub "${ProjectNamespace}-${Environment}-MessagingPlatformTableName"
        - !Sub "${ProjectNamespace}-MessagingPlatformTableName"
  MessagingPlatformTableArn:
    Description: "The ARN of the DynamoDB table MessagingPlatform"
    Value: !GetAtt MessagingPlatform.Arn
    Export:
      Name: !If
        - HasEnvPrefix
        - !Sub "${ProjectNamespace}-${Environment}-MessagingPlatformTableArn"
        - !Sub "${ProjectNamespace}-MessagingPlatformTableArn"
  TCGMarketplaceTableName:
    Description: "The name of the DynamoDB table TCGMarketplace"
    Value: !Ref TCGMarketplace
    Export:
      Name: !If
        - HasEnvPrefix
        - !Sub "${ProjectNamespace}-${Environment}-TCGMarketplaceTableName"
        - !Sub "${ProjectNamespace}-TCGMarketplaceTableName"
  TCGMarketplaceTableArn:
    Description: "The ARN of the DynamoDB table TCGMarketplace"
    Value: !GetAtt TCGMarketplace.Arn
    Export:
      Name: !If
        - HasEnvPrefix
        - !Sub "${ProjectNamespace}-${Environment}-TCGMarketplaceTableArn"
        - !Sub "${ProjectNamespace}-TCGMarketplaceTableArn"
  UserProfileTableName:
    Description: "The name of the DynamoDB table UserProfile"
    Value: !Ref UserProfile
    Export:
      Name: !If
        - HasEnvPrefix
        - !Sub "${ProjectNamespace}-${Environment}-UserProfileTableName"
        - !Sub "${ProjectNamespace}-UserProfileTableName"
  UserProfileTableArn:
    Description: "The ARN of the DynamoDB table UserProfile"
    Value: !GetAtt UserProfile.Arn
    Export:
      Name: !If
        - HasEnvPrefix
        - !Sub "${ProjectNamespace}-${Environment}-UserProfileTableArn"
        - !Sub "${ProjectNamespace}-UserProfileTableArn"
